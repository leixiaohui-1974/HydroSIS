[pytest]
python_files = test_*.py
python_classes = Test*
python_functions = test_*
testpaths = tests
addopts = -v --tb=short --strict-markers -ra

markers =
    unit: 单元测试
    integration: 集成测试
    slow: 慢速测试
    validation: 验证框架测试
    parallel: 并行化测试
